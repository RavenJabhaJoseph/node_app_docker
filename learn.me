1. A bridge network allows containers in the same network to communicate
2. Use the docker network create command to create a bridge network
3. Add containers to a network using docker run along with the --net switch
4. Docker Compose is an orchestration engine that can build and run containers
